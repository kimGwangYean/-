#include <stdio.h>
#include <stdlib.h>
int pro(int n, int m, int k)
{
    int *table,i,t=m;
    table=(int*)malloc(sizeof(int)*n);
    for(i=0;i<n;i++)
    {
        table[i]=i+1;
    }
    while(1)
    {

        break;
    }
}
