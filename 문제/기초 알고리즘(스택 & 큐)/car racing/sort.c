#include <stdio.h>
#include <stdlib.h>
int sort(int n,int *order)
{
    int i=0,c=0;
    int *imsi,*result;
    imsi=(int*)malloc(sizeof(int)*n);
    result=(int*)malloc(sizeof(int)*n);
    while(1)
    {

    }
    return result;
}
